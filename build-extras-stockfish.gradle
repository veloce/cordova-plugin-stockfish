android.defaultConfig {
    ndk {
        abiFilters 'armeabi-v7a', 'arm64-v8a', 'x86', 'x86_64'
    }
}

android.externalNativeBuild {
    ndkBuild {
        path '../capacitor-cordova-android-plugins/src/main/jni/Android.mk'
    }
}
